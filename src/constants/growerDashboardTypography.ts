/**
 * Grower dashboard home typography — Inter only.
 * Regular (400) body · Medium (500) labels/buttons · Semibold/Bold (600–700) headings
 */
export const GD_FONT = 'font-inter';

/** Page & section headings */
export const GD_H1 = `${GD_FONT} text-[17px] sm:text-xl lg:text-2xl font-bold text-[#002f37] tracking-tight leading-tight`;
export const GD_H2 = `${GD_FONT} text-sm sm:text-base lg:text-lg font-semibold text-[#002f37] tracking-tight`;
export const GD_H3 = `${GD_FONT} text-base sm:text-lg font-semibold text-[#002f37] tracking-tight`;

/** Eyebrow / overline labels */
export const GD_EYEBROW = `${GD_FONT} text-[10px] font-medium uppercase tracking-[0.18em] text-[#065f46]`;
export const GD_EYEBROW_LIGHT = `${GD_FONT} text-[10px] font-medium uppercase tracking-[0.16em]`;

/** Body copy */
export const GD_BODY = `${GD_FONT} text-sm font-normal text-gray-600 leading-relaxed`;
export const GD_BODY_SM = `${GD_FONT} text-xs font-normal text-gray-500 leading-snug`;
export const GD_CAPTION = `${GD_FONT} text-[11px] font-normal text-gray-500 leading-snug`;

/** Labels & metadata */
export const GD_LABEL = `${GD_FONT} text-xs font-medium text-gray-500`;
export const GD_LABEL_SM = `${GD_FONT} text-[10px] font-medium text-gray-500`;
export const GD_ID = `${GD_FONT} text-[11px] font-medium text-[#065f46] tabular-nums tracking-wide`;

/** Buttons & interactive text */
export const GD_BTN = `${GD_FONT} text-sm font-medium`;
export const GD_BTN_SM = `${GD_FONT} text-xs font-medium`;
export const GD_BTN_PILL = `${GD_FONT} text-[10px] font-medium uppercase tracking-wide`;

/** Metric cards (colored tiles) */
export const GD_METRIC_LABEL = `${GD_FONT} text-[8px] sm:text-[10px] lg:text-[11px] font-medium uppercase tracking-[0.12em] text-white/85 leading-none`;
export const GD_METRIC_VALUE = `${GD_FONT} text-[17px] sm:text-xl lg:text-4xl font-bold text-white leading-none tracking-tight tabular-nums`;
export const GD_METRIC_SUB = `${GD_FONT} text-[8px] sm:text-[10px] lg:text-xs font-normal text-white/60 uppercase tracking-wide leading-snug`;

/** Hero status card */
export const GD_HERO_EYEBROW = `${GD_FONT} text-[9px] sm:text-[10px] font-medium uppercase tracking-[0.2em]`;
export const GD_HERO_TITLE = `${GD_FONT} text-base sm:text-xl lg:text-2xl font-bold tracking-tight leading-tight`;
export const GD_HERO_BODY = `${GD_FONT} text-[11px] sm:text-xs font-normal leading-snug`;

/** Snapshot / chart card */
export const GD_CARD_TITLE = `${GD_FONT} text-xl sm:text-2xl font-bold text-[#002f37] tracking-tight`;
export const GD_CARD_META = `${GD_FONT} text-xs sm:text-sm font-normal text-gray-600`;
export const GD_STAT_VALUE = `${GD_FONT} font-bold text-[#065f46] tabular-nums leading-none`;
export const GD_STAT_LABEL = `${GD_FONT} text-[10px] font-medium text-gray-500 uppercase tracking-wide`;

/** Root wrapper for dashboard home page */
export const GROWER_DASHBOARD_HOME = 'grower-dashboard-home';
